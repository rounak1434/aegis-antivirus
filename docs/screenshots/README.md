# docs/screenshots

Placeholder directory for UI screenshots referenced by the project README.

The expected files (see [`../../SCREENSHOTS.md`](../../SCREENSHOTS.md) for the
full inventory and capture steps):

- `dashboard.png`
- `scan-center.png`
- `threat-center.png`
- `quarantine.png`
- `realtime.png`
- `settings.png`

These are not yet committed — the React UI is mid-migration. Until then the
README image links resolve to nothing (placeholders). Capture them from the
running app or the prototype as described in `SCREENSHOTS.md`, drop the PNGs
here, and they will render automatically.
